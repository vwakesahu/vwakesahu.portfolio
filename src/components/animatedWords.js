import WordRotate from "./ui/wordRotate";

export async function WordRotateComponent() {
  return (
    <WordRotate
      className="text-4xl font-bold text-black dark:text-white"
      words={["Word", "Rotate"]}
    />
  );
}
